<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <!-- Disable CET (Hardware-enforced Stack Protection) to prevent
         STATUS_STACK_BUFFER_OVERRUN crash when shell extensions (image
         thumbnail providers) load in file dialogs. These older DLLs
         aren't CET-compatible and trigger shadow stack violations. -->
    <CetCompat>false</CetCompat>
    <UseWPF>false</UseWPF>
    <!-- Application Icon -->
    <ApplicationIcon>favicon.ico</ApplicationIcon>
    <!-- Version Information -->
    <Version>4.1</Version>
    <AssemblyVersion>4.1.0</AssemblyVersion>
    <FileVersion>4.1.0</FileVersion>
    <InformationalVersion>4.1</InformationalVersion>
    <!-- Application Metadata -->
    <Product>D3dxSkinManager</Product>
    <Company>JiarongGu</Company>
    <Copyright>Copyright © 2026 JiarongGu</Copyright>
    <Description>A modern skin management tool for 3DMigoto-based game mods</Description>
  </PropertyGroup>
  <!-- Release configuration: Disable debug symbols and hide console window -->
  <PropertyGroup Condition="'$(Configuration)' == 'Release'">
    <DebugType>none</DebugType>
    <DebugSymbols>false</DebugSymbols>
    <!-- WinExe hides the console window in production -->
    <OutputType>WinExe</OutputType>
  </PropertyGroup>
  <ItemGroup>
    <!-- Shared contracts + host abstractions. The host implements these interfaces
         (MessageDispatcher/EventBus/PluginContext/LogHelper). The plugin SDK also references Core, so the
         host never references a plugin project. See plugin-system.md. -->
    <ProjectReference Include="..\D3dxSkinManager.Core\D3dxSkinManager.Core.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Costura.Fody" Version="6.0.0">
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Dapper" Version="2.1.72" />
    <PackageReference Include="FluentMigrator" Version="8.0.1" />
    <PackageReference Include="FluentMigrator.Runner.SQLite" Version="8.0.1" />
    <PackageReference Include="Microsoft.Data.Sqlite" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Caching.Memory" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Logging" Version="10.0.0" />
    <PackageReference Include="Microsoft.Web.WebView2" Version="1.0.3800.47" />
    <PackageReference Include="SharpSevenZip" Version="*" />
    <PackageReference Include="SixLabors.ImageSharp" Version="3.1.12" />
    <PackageReference Include="System.Security.Cryptography.ProtectedData" Version="10.0.9" />
  </ItemGroup>
  <!-- Embedded resources (inside DLL/EXE) -->
  <ItemGroup>
    <!-- Embed wwwroot files as embedded resources -->
    <EmbeddedResource Include="wwwroot\**\*.*" Condition="Exists('wwwroot')">
      <LogicalName>D3dxSkinManager.wwwroot.%(RecursiveDir)%(Filename)%(Extension)</LogicalName>
    </EmbeddedResource>
    <!-- Embed application icon -->
    <EmbeddedResource Include="favicon.ico">
      <LogicalName>D3dxSkinManager.favicon.ico</LogicalName>
    </EmbeddedResource>
  </ItemGroup>
  <!-- Language files (separate files next to exe) -->
  <ItemGroup>
    <Folder Include="Languages\" />
    <Content Include="Languages\**\*.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
      <TargetPath>res\languages\%(RecursiveDir)%(Filename)%(Extension)</TargetPath>
    </Content>
    <!-- Remote-library adapter default configs (seeds). Ship under the top-level res\ folder
         (NOT user data\); RemoteSourceStore copies any missing one into {data}/remote-sources on first run. -->
    <Content Include="RemoteSources\**\*.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
      <TargetPath>res\remote-sources\%(RecursiveDir)%(Filename)%(Extension)</TargetPath>
    </Content>
    <!-- Shipped default release/update locations (res\endpoints.json). Repo-managed + released together
         with the remote-source seeds; ReleaseEndpointConfig reads it, and an operator's
         data\settings\endpoints.json overrides per-field. -->
    <Content Include="Resources\endpoints.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
      <TargetPath>res\endpoints.json</TargetPath>
    </Content>
  </ItemGroup>
  <!-- Native libraries (7z.dll - architecture-specific, kept separate for runtime loading) -->
  <ItemGroup>
    <!-- Copy x64 7z.dll for 64-bit builds (default when Platform is not specified or is x64/AnyCPU) -->
    <Content Include="libs\x64\7z.dll" Condition="'$(Platform)' == 'x64' OR '$(Platform)' == 'Any CPU' OR '$(Platform)' == '' OR '$(Platform)' == 'AnyCPU'">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
      <Link>libs\7z.dll</Link>
      <TargetPath>libs\7z.dll</TargetPath>
    </Content>
    <!-- Copy x86 7z.dll for 32-bit builds -->
    <Content Include="libs\x86\7z.dll" Condition="'$(Platform)' == 'x86'">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
      <Link>libs\7z.dll</Link>
      <TargetPath>libs\7z.dll</TargetPath>
    </Content>
  </ItemGroup>
  <!-- Single-file publish configuration -->
  <PropertyGroup>
    <!-- Enable single-file publish -->
    <PublishSingleFile>true</PublishSingleFile>
    <!-- Include native libraries for self-extract -->
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    <!-- DO NOT extract content files - keep them separate -->
    <IncludeAllContentForSelfExtract>false</IncludeAllContentForSelfExtract>
    <!-- Enable ReadyToRun for faster startup -->
    <PublishReadyToRun>true</PublishReadyToRun>
    <!-- Trim unused code (be careful with reflection-based code) -->
    <PublishTrimmed>false</PublishTrimmed>
    <!-- Disable XML documentation output -->
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
    <PublishDocumentationFile>false</PublishDocumentationFile>
    <!-- Don't copy XML docs from dependencies -->
    <AllowedReferenceRelatedFileExtensions>none</AllowedReferenceRelatedFileExtensions>
  </PropertyGroup>
</Project>